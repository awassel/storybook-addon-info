import { create } from '@storybook/theming';

export default create({
  // Is this a 'light' or 'dark' theme?
  base: 'dark',

  // Typography
  fontCode: 'monospace',

  // Brand logo/text
  brandTitle: 'Storybook Addon Info',
  brandUrl: 'http://www.addamwassel.com/',
});
